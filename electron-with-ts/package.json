{
  "dependencies": {
    "copyfiles": "^2.4.1",
    "electron": "^12.0.7",
    "typescript": "^4.2.4"
  },
  "scripts": {
    "compile": "tsc && copyfiles -f src/index.html build",
    "start": "npm run compile && electron ."
  }
}
